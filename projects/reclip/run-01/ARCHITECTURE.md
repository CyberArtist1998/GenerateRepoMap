# ARCHITECTURE

_Generated by repo_graph.py at commit `297b79d`. 6 source files, 0 dependencies._

If the commit above is not HEAD, this map is stale - regenerate before trusting it.

## Start here (entry points)

- `app/app.py` (imports 0 files)
- `install.js` (imports 0 files)
- `pinokio.js` (imports 0 files)
- `reset.js` (imports 0 files)
- `start.js` (imports 0 files)
- `update.js` (imports 0 files)

## Subsystems

Folders are the grouping; cohesion measures whether the folder is honest.
Compare groups against EACH OTHER, not against 1.0 - in a healthy layered
codebase every group leans on shared core, so real subsystems commonly sit
around 0.2-0.4. A group near 0.0 is a bucket of unrelated files.

| Group | Files | Cohesion | AI: what is this? |
|---|---:|---:|---|
| `(root)` | 5 | 0.0 | _(unlabelled)_ |

## Confidence

- static (certain): 0
- dynamic (inferred from a name prefix): 0
- runtime (observed in a real run): 0

**No runtime trace was merged.** Dynamic wiring is inferred, not observed. For ground truth run the app once with `-X importtime` and pass `--import-trace` (see MANUAL.md step 4).

